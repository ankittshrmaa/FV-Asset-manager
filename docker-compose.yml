version: '3.8'

services:
  asset-manager:
    build: .
    container_name: fv-asset-manager
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped